<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="basededatosconrooma">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BaseDeDatosConRoomB">
        <activity
            android:name="net.ivanvega.basededatosconrooma.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

            <provider
                android:authorities="basededatosconrooma.provider"
                android:name="net.ivanvega.basededatosconrooma.provider.MiProveedorContenido"
                android:exported="true"
                >

        </provider>
    </application>

</manifest>
